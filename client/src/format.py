def format_headlines_json(json_data: dict) -> str:
    """
        `format_headlines_json` formats the json data to a string.
        It takes in the json data and returns the formatted string.

        Params:
            json_data (dict): The json data to format.

        Returns:
            `str`: The formatted string.
    """
    
